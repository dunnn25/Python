import pickle

dict = {
    "name": "Dung",
    "age": 21,
    "Job": "AI Enginer"
}
# save python object to .pickle file
# with open("save.pickel", "wb") as stream:
    # pickle.dump(dict, stream)

# load content of .pickle file   
# with open("save.pickel","rb") as stream:
#     my_dict = pickle.load(stream)
# print(my_dict)
# print(type(my_dict))
 
 
# pricle_ob = pickle.dumps(dict)
# new_dict = pickle.loads(pricle_ob)